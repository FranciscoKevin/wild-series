{% extends 'base.html.twig' %}
{%  block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ actor.name }}</h1>
            <div>
                <p class="remove-margin">List of series by this actor :</p>
                <ul>
                    {% for program in actor.programs %}
                        <li>
                            <a href="{{ path("program_show", {'program_id': program.id})}}">{{ program.title }}</a>
                        </li>

                    {% else %}
                        <li>No actor for this series</li>
                    {% endfor %}
                </ul>

            </div>
            <a class="link-return" href="{{ path('actor_index') }}"> Back to the list of actors</a>
        </div>
    </div>
{% endblock %}